import styles from "../styles/section.module.css";
const Section = () => {
return (
    <div className={`${styles["section"]}`}>
        <h1>Mac</h1>
        <h2>If you can dream it, Mac can do it.</h2>
    </div>
)
}

export default Section;
