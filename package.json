{
  "name": "apple_recreation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react-dom": "^19.0.0"
  }
}
